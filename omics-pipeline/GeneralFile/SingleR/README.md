# SingleR references

Bundled references used by `Script/lib/scRNA_core.R`:

| Organism | File |
|----------|------|
| Human (`hsa`) | `HumanPrimaryCellAtlasData.rds` |
| Mouse (`mmu`) | `MouseRNAseqData.rds` |

Additional celldex / SingleR `.rds` files can be placed here locally if needed.
